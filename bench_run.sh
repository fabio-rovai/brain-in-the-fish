#!/usr/bin/env bash
# Subagent benchmark runner
# This script is executed by Claude Code's orchestration, not directly.
# See BENCHMARK.md for usage.
echo "This benchmark is orchestrated by Claude Code subagents."
echo "Run it from a Claude Code session — the subagents ARE the LLM."
